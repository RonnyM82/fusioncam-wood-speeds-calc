// The React page's Stylelint config, extended from the package and never
// copied. The lint script names src/ as the folder it reads, so the Fusion
// panel's fusion.css and the vanilla styles.css stay with conformance.py.
export default { extends: ["@livetools/ui/stylelint"] };
